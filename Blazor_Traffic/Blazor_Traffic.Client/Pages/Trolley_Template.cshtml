@using Blazor_Traffic.Shared

<div class="row">
    <div class="col-md">
        <table class="table">
            <thead>
                <tr align="center">
                    <th colspan="4">
                        Тролейбуси:
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var trolley in this.Trolleys)
                {
                <tr>
                    <td class="col-md-3 col-xs-3" title="@trolley.TransportType" align="left">
                        <span class="sm-label d-flex d-md-inline-block p-1 w-md-35 trolley">
                            <span class="oi oi-vertical-align-top p-1 mr-1" aria-hidden="true"></span>
                            <span>@trolley.LineName</span>
                        </span>
                    </td>
                    @foreach (var time in trolley.Times)
                    {
                        <td class="col-md-3 col-xs-3 align-middle" align="center">
                            @time.ToShortTimeString()
                        </td>
                    }
                    @if (trolley.Times.Length < 3)
                    {
                        var loops = 3 - trolley.Times.Length;

                        for (int i = 0; i < loops; i++)
                        {
                            <td class="col-md-3 col-xs-3 align-middle" align="center"></td>
                        }
                    }
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@functions {
    [Parameter]
    private StopTimes[] Trolleys { get; set; }
}